#ifndef KCONFIG_H
#define KCONFIG_H

/* 
 * K-ABI Debugging Toggle
 * 
 * Define KABI_DEBUG to enable:
 * - Boot-time diagnostic test suites (Core & Module tests)
 * - Interactive kernel shell commands (TEST, STRESS, CORE)
 * - Verbose syscall tracing
 */
#define KABI_DEBUG 1

#endif
